# JanVedha AI — Backend Dependencies
# Python 3.11+

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
pydantic-settings
pydantic[email]
python-multipart

# ── MongoDB ───────────────────────────────────────────────────────────────────
motor>=3.3.0
beanie>=1.25.0

# ── AI / LLM / LangChain ──────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-google-genai>=1.0.0
langchain-community>=0.2.0

# ── ML / Priority Model (LightGBM v2) ────────────────────────────────────────
scikit-learn>=1.4.0          # Fallback if lightgbm unavailable
numpy>=1.26.0
joblib>=1.3.0
lightgbm>=4.3.0              # Priority scoring: non-linear, SHAP-explainable
shap>=0.45.0                 # Explainability for LightGBM priority predictions

# ── Time Series / Seasonal Prediction ────────────────────────────────────────
prophet>=1.1.5               # Seasonal spike forecasting (requires pystan)

# ── NLP / Multilingual Sentiment (IndicBERT) ─────────────────────────────────
transformers>=4.41.0         # HuggingFace model hub
# torch: install CPU-only build to save ~1.5GB:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.3.0

# ── Social Media Scraping ─────────────────────────────────────────────────────
asyncpraw>=7.7.0             # Async Reddit scraper (requires REDDIT_CLIENT_ID in .env)

# ── Auth & Security ───────────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
python-jose[cryptography]
PyJWT==2.8.0

# ── Caching / Sessions ────────────────────────────────────────────────────────
redis==5.0.3

# ── Notifications ─────────────────────────────────────────────────────────────
twilio
sendgrid

# ── Task Queue ────────────────────────────────────────────────────────────────
celery==5.3.6

# ── NLP / Language Detection ──────────────────────────────────────────────────
langdetect

# ── Storage ───────────────────────────────────────────────────────────────────
aiofiles

# ── HTTP ──────────────────────────────────────────────────────────────────────
httpx
beautifulsoup4

# ── Documents / Templates ─────────────────────────────────────────────────────
jinja2
weasyprint

# ── Blockchain (optional, stub available) ─────────────────────────────────────
web3
